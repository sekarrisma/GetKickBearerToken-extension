"use strict";chrome.runtime.onMessage.addListener(e=>{if(typeof e.session_token=="string"){let t=document.getElementById("token");t&&(t.value=e.session_token)}});let button=document.getElementById("reloadBtn");button==null||button.addEventListener("click",async()=>{let[e]=await chrome.tabs.query({active:!0,currentWindow:!0});e!=null&&e.id&&chrome.runtime.sendMessage({action:"reload-and-check-cookie",tabId:e.id})});
